export function tco(car){
    var total = deprecation(car) + fuel(car) + interest(car) + tax(car) + insurance(car) + maintenance(car) + subventions(car);
    return total
}

export function deprecation(car) {
    var value = car;
    return value
}

export function fuel(car) {
    var value = car;
    return value
}

export function interest(car) {
    var value = car;
    return value
}

export function tax(car) {
    var value = car;
    return value
}

export function insurance(car) {
    var value = car;
    return value
}

export function maintenance(car) {
    var value = car;
    return value
}

export function subventions(car) {
    var value = car;
    return value
}